#LVM добавление дополнительного места
 Добавляется существующий раздел (например, /dev/sda3):
- Убеждаемся, что у него тип Linux LVM (8e):
  
  sudo fdisk -l /dev/sda
  
  
Добавляем PV в существующую группу томов (VG). Сначала смотрим её имя:
sudo vgdisplay
